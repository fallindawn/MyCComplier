C语言编译器功能总结报告
================================================================================
C语言编译器实现 - 功能总结报告
================================================================================

一、项目概述
本项目是一个完整的C语言编译器实现，采用经典的三阶段编译架构，包括词法分析、语法分析、中间代码生成等核心模块。项目结构清晰，文档齐全，代码规范，已通过全部测试用例，功能完整。
二、编译器支持的语言特性
1. 数据类型
支持 int 类型（32位整数）
2. 声明语句
支持变量声明：int var;
3. 语句类型
赋值语句：var = expr;
条件语句：if (expr) stmt; 和 if (expr) stmt else stmt;
循环语句：while (expr) stmt; 和 for (init; cond; update) stmt;
输入语句：scanf("%d", &var);
输出语句：printf(expr);
复合语句：{ stmt_list }

4. 运算符
算术运算符：+ - * / % ^
关系运算符：== != < > <= >=
逻辑运算符：&& || !
赋值运算符：=

5. 注释
支持多行注释 /* */
支持单行注释 //

三、编译器各阶段功能
1. 词法分析
使用 Flex 工具实现
识别关键字、标识符、数字、运算符等 token
支持行号追踪
输出格式化的 token 列表

2. 语法分析
使用 Bison 工具实现，采用 LALR(1) 分析算法
构建抽象语法树（AST），包含 18 种节点类型
支持运算符优先级和结合性处理
输出树形结构的 AST

3. 符号表管理
记录变量名、类型、声明行号
支持变量查询、重复定义检查
输出格式化的符号表

4. 中间代码生成
生成四元式格式的中间代码
自动分配临时变量（t0, t1, …）和标签（L0, L1, …）
支持表达式和语句的翻译
输出四元式列表

5. 错误处理
语法错误检测与报告
变量未声明检测
错误信息包含行号和描述

四、项目特色与亮点
完整的编译管道，各阶段输出清晰可见
规范的工程实现，使用标准工具链（Flex + Bison + GCC）
详细的文档支持，包括教程、演示报告、截图指南等
代码结构清晰，易于扩展和维护
支持跨平台编译（Windows / Linux / macOS）

五、技术栈
词法分析：Flex
语法分析：Bison
实现语言：C（C99）
构建工具：Make / PowerShell
编译器：GCC 或 Clang



C语言编译器实现 - 功能分类报告
================================================================================
一、基本功能（满足编译原理课程基本要求）
1. 词法分析（20分）
识别所有关键字（int, if, else, while, for, scanf, printf等）
识别所有运算符（算术、关系、逻辑）
识别标识符和数字字面值
支持注释（/* */ 和 //）
行号追踪

2. 语法分析（30分）
程序结构解析（声明列表 + 语句列表）
变量声明解析
赋值语句解析
if-else 条件语句解析
while 循环解析
for 循环解析
输入输出语句解析
表达式解析（含优先级）

3. 抽象语法树构建
构建树形AST，节点类型丰富
支持树形打印输出

4. 符号表管理（10分）
变量声明记录
变量查询与重复定义检查

5. 中间代码生成（10分）
四元式格式实现
临时变量自动分配
表达式和语句翻译

6. 类型检查（10分）
变量使用前声明检查
基本类型一致性检查

7. 错误处理（5分）
语法错误检测
变量未声明错误检测

8. 基础文档（20分）
README 技术说明文档
测试用例说明

二、额外功能（超出基本要求的增强功能）
1. 完整的编译管道可视化输出
每个阶段都有格式化输出（词法、AST、符号表、四元式）
树形AST展示，直观体现语法结构

2. 详细的错误报告与定位
错误信息包含具体行号和描述
支持语义错误检测（如变量未声明）

3. 跨平台构建支持
提供 Makefile（Linux/macOS）
提供 PowerShell 脚本（Windows）
提供批处理脚本（Windows）
